@model IEnumerable<MVCKhumaloCraftFinal2.Models.Checkout>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CartItem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CartItem.CartItemID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.checkoutID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.checkoutID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.checkoutID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
